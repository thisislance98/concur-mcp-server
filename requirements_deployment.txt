# Core dependencies for Concur MCP Server
# Use this for deployment instead of requirements.txt to avoid git+https issues

# FastMCP framework
fastmcp>=2.11.3

# HTTP requests
requests>=2.32.5

# Environment variable management
python-dotenv>=1.1.1

# Optional: OAuth support (comment out if causing deployment issues)
# git+https://github.com/peterlarnholt/fastmcp-oauth.git
